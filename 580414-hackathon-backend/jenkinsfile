pipeline {
   agent any
   tools {
        maven 'maven 5.4.1'
        jdk 'jdk8'
   }
    stage('maven Build') {
        if (isUnix()) {
            sh './mvn clean build'
        } else {
            bat 'mvn clean build'
        }
    }
}
